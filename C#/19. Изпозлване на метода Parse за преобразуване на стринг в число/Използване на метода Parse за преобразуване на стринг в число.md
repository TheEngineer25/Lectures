В тази лекция ние ще разгледаме `парсването`.  
`Парсването` ни позволява да преобразуваме данни от един тип в данни от друг тип. Това обаче не работи с всичко наред. При този подход можем много лесно да се сблъскаме с грешки. Затова трябва да се уверим, че сме въвели правилния тип, защото в противен случай програмата ни ще се срине.
Засега всичко е добре. В бъдеще обаче, след няколко часа, ще разгледаме подходи, чрез които можем да отстраним тези грешки напълно и дори да не се сблъскваме с тези проблеми.
И така, как можем а преобразуваме каквото и да получим от `Console.ReadLine()`, което е от тип `string`, както вече знаем, в тип данни `int`, където да можем да съхраняваме цели числа?
Има нещо, наречено `парсване`. Така че ние можем да използваме
> [!info] 💻Лекция: Разбиране на типа данни int и защо той не може да побере string
>Използване на парсинг
> ```csharp
> myNumber = int.Parse(Console.ReadLine());
> ```

Ние обаче можем и по-добре. Преместваме този ред по-долу и го заместваме по следния начин.
> [!info] 💻Лекция: Разбиране на типа данни int и защо той не може да побере string
>Използване на парсинг
> ```csharp
> myNumber = int.Parse(userInput);
> ```

Това е абсолютно същото, все едно сме написали по-горния ред. И двата реда ще ни дадат еднакъв резултат. 
И така, този `парсинг` ще работи само ако сме въвели число. Ще го стартираме и ще въведем някакво число.
![[19. Изпозлване на метода Parse за преобразуване на стринг в число/Изображения/1.png]]
Казва ни се `You entered 7`. Изглежда, че работи. Програмата ни не се срива.
нека да повторим това, но този път ще въведем текст.
![[19. Изпозлване на метода Parse за преобразуване на стринг в число/Изображения/2.png]]
Получаваме грешка. Казва ни се, че входните данни `Привет` не са в правилния формат, понеже това не е число и няма как да преобразуваме този текст в число. Ние трябва да въведем цяло число.
Ако ние опитаме да въведем нещо като например 2.7, ще получим подобна грешка, защото въпреки че сме въвели число, то не е цяло число. Може би трябва да сме по-точно и да изискваме да се въвеждат само цели числа.
> [!info] 💻Лекция: Използване на метода Parse за преобразуване на стринг в число
>Промяна на съобщението
> ```csharp
> Console.WriteLine("Enter a whole number!");
> ```

Нека да стартираме това отново и сега ще въведем 17.
![[19. Изпозлване на метода Parse за преобразуване на стринг в число/Изображения/3.png]]И така, т ова работи. Сега видяхме как да използваме `парсинга`, за да вземем входните данни, които са от тип стринг и да ги преобразуваме в число. По-късно ще видим как можем да направим това и за числа с десетична част.